# Examples

The primary example is the checked-in local fixture used by `evaluate.py`.
It demonstrates SBOM component normalization, vulnerability matching, dependency context, provenance signals, and CI policy decisions without requiring external accounts or
credentials. Add new examples with expected outcomes and a short explanation.
