# Local Operations

Run the project in an isolated virtual environment. Keep optional credentials
in environment variables, use demo mode by default, and inspect generated
results before changing any integration. The project is designed for local
defensive analysis and recommendation-only workflows.
