# Security Controls

The project uses input validation, bounded local execution, no embedded
credentials, reproducible tests, and recommendation-only behavior where an
analyst or operator would otherwise need to approve an action.
