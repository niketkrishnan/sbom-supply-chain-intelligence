# Release Process

Before a release, run the full test suite, run the evaluation command, inspect
generated output, review dependency changes, confirm that no secrets are
tracked, and update the changelog with limitations and data provenance.
